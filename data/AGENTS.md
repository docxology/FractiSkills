# data/ — hand-authored inputs

- `sources/` — declarative site specs; `ssvibelandia.yaml` bounds every
  network behavior (crawl limits, user agent, dynamic bindings).
- This tree is reviewed input, not generated output; nothing here is
  regenerated by the pipeline.

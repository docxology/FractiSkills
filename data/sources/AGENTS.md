# data/sources/ — site specs

`ssvibelandia.yaml` is the single source of network truth for FractiSkills:
base URL, sitemap URL, user agent, discovery/crawl limits, extraction caps,
augmentation timeout, and the dynamic-content bindings. Edit with care and
re-run the full pipeline after any change; every binding here is a reviewed
same-origin endpoint contract.

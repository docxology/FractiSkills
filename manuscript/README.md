# manuscript/

Token-bound manuscript sources. Chapters stay pristine; `{{TOKEN}}` values
are computed by `fractiskills.publication` and bound into `output/manuscript/`.
Rendered by the parent template engine once the project is linked.

# src/fractiskills/

The whole implementation: deterministic contracts (`models`, `profiles`),
acquisition boundary (`discover`), augmentation boundary (`generator`),
orchestration (`pipeline`), research layer (`analysis`, `figures`,
`publication`), and CLI dispatch (`cli`). See AGENTS.md for invariants.
